# configs/

Declarative experiment/run configurations (YAML).

Naming: `<milestone>_<model>_<variant>.yaml` (e.g. `m2_occ_unet_v0.yaml`).
See `docs/03_EXPERIMENT_PROTOCOL.md`. Configs should be self-contained and
versioned; experiments record the exact config they used.

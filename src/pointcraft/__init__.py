"""PointCraft — Aerial-to-Embodied Semantic Scene Completion.

New research code lives here (data, voxelization, models, losses, metrics,
mc_export, utils). The legacy deterministic pipeline is the separate repo-root
``pointcraft`` package and serves as the M1 baseline. See CLAUDE.md.
"""

__version__ = "0.1.0"
